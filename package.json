{
  "name": "soulmegle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run build --prefix backend\" \"npm start --prefix frontend\"",
    "start": "npm run start --prefix backend"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
